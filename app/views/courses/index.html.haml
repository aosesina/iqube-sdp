%h1 Listing courses

%table.table.table-stripped.table-bordered
  %tr
    %th Code
    %th Title
    %th Summary
    %th Price
    %th Duration in weeks
    %th
    %th
    %th

  - @courses.each do |course|
    %tr
      %td= course.code
      %td= course.title
      %td= course.summary
      %td= course.price
      %td= course.duration_in_weeks
      %td= link_to 'Show', course,:class=>"btn"
      %td= link_to 'Edit', edit_course_path(course), :class=>"btn"
      %td= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' }, :class=>"btn"

%br

= link_to 'New Course', new_course_path, :class=>"btn"
